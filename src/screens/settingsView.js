import { SafeAreaView, Text, View } from 'react-native';

const SettingsView = () => {
    return (
        <SafeAreaView>
        <View>
            <Text>
                Settings
            </Text>
        </View>
        </SafeAreaView>
    );
};

export default SettingsView;
